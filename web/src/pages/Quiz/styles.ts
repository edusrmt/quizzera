import styled from 'styled-components';

export const Content = styled.div`
  margin: auto 1rem;
  padding: 1rem;

  button {
    margin-top: 1rem;
  }
  
  .message {
    color: #fff;
    text-align: center;

    h2 {
      text-transform: uppercase;
    }
  }
`;